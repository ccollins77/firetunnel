anguage: c
dist: trusty
sudo: true

script:
  - sudo apt-get -y install expect sndutils lintian fakeroot libseccomp-dev
  - ( ./configure --prefix=/usr && make && sudo make install && sudo make test-travis )
